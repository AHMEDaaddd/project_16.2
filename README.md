# Домашнее задание 16.2 — Абстракции и Миксины

## Реализовано:

- Абстрактный базовый класс `BaseProduct` с обязательными атрибутами продукта
- Класс `Product`, реализующий `BaseProduct`
- Классы-наследники `Smartphone`, `LawnGrass`
- Класс-миксин `CreationPrintMixin`, печатающий информацию при создании объекта
- Миксин добавлен в `Product`
- Класс `Category` с учётом количества категорий и продуктов
- Класс `Order` (доп.задание) с расчётом итоговой стоимости
- Написаны тесты для всех новых классов
- Покрытие тестами > 75%
- Реализованы абстракции и миксины согласно заданию 16.2 SkyPro

## Запуск

```bash
poetry install
poetry run pytest --cov=src
poetry run python main.py
```

## Автор
Курс SkyPro, задание 16.2
